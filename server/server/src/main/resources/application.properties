# src/main/resources/application.properties

# --- server ---
server.port=8080

# --- datasource: H2 (dev only) ---
spring.datasource.url=jdbc:h2:mem:delifay;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.driver-class-name=org.h2.Driver
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# --- JPA (dev) ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- CORS ---
app.cors-allowed-origins=http://localhost:5173,http://localhost:3000

# --- National Library of Korea Open API ---
nl.api.key=<???????API? ??????>
nl.api.holdings.base=https://www.nl.go.kr/NL/search/openApi/search.do
nl.api.isbn.base=https://www.nl.go.kr/seoji/SearchApi.do

