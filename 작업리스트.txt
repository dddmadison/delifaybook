다음 우선순위 1: 전역 상태로 “내 책장”을 하나로 만들기

해야 할 것 3개

BookContext(또는 Store) 만들기

내 책 목록(bookshelf)

진행도(progress)

메모(notes)

검색에서 [추가] 누르면 bookshelf에 들어가게

상세에서 진행 업데이트/메모 추가하면 동일 데이터가 갱신되게

다시 책장/홈으로 가도 반영됨

→ 이거까지 하면 “앱처럼” 동작하기 시작한다.

다음 우선순위 2: 라우팅 UX 정리

상단 공통 네비(홈/책장/검색) 컴포넌트로 분리

책 상세에서 뒤로 가기 동작 안정화

TopNav를 공통 컴포넌트로 분리
AppLayout을 만들고 Home/Bookshelf/Search를 그 레이아웃 아래로 넣기
BookDetailPage는 “뒤로” 동작을 from 우선 → fallback 규칙으로 통일
(선택) Search는 검색어를 URL에 남기기(뒤로/새로고침 안정화)



다음 우선순위 3: (나중) Spring 붙이기 준비

API 호출 함수 파일 분리

mock → api로 교체하기 쉽게 구조 정리


페이지 컴포넌트는 API를 직접 호출하지 않는다.
API 호출은 Context(BooksProvider) 안에서만 한다.
mock/api 전환은 Context에서 한 번에 스위치된다.